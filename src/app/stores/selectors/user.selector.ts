// import { createSelector } from '@ngrx/store';
// // import { LabelsState, getMailAppState, MailAppState } from 'app/main/apps/mail-ngrx/store/reducers';
// import { UserState,UserReducer, getAppState,getUserAppState} from '../reducers/user.reducer';
//
// export const getUserState = createSelector(
//     getUserAppState,
//     (state: UserState) => state.user
// );
//
// export const getUserTokenState = createSelector(
//     getUserAppState,
//     (state: UserState) => state.token
// );
//
//
// export const getUserPermissionsState = createSelector(
//     getUserAppState,
//     (state: UserState) => state.permissions
// );

// export const getLabels = createSelector(
//     getLabelsState,
//     (state: LabelsState) => state.entities
// );

// export const getLabelsLoaded = createSelector(
//     getLabelsState,
//     (state: LabelsState) => state.loaded
// );

// export const getLabelsArr = createSelector(
//     getLabels,
//     (entities) => Object.keys(entities).map((id) => entities[id])
// );
